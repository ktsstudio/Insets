<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.ktsstudio.sample.insets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Insetsexamples"
        tools:ignore="AllowBackup">
        <activity
            android:name=".ui.menu.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Insetsexamples.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.samples.tappable.TappableSampleActivity" />
        <activity android:name=".ui.samples.basic_list.ListSampleActivity" />
        <activity android:name=".ui.samples.gestures.GestureExclusionComposeActivity" />
        <activity android:name=".ui.samples.gestures.MandatoryGestureComposeActivity" />
        <activity android:name=".ui.samples.keyboard.KeyboardBasicImeComposeActivity" />
        <activity android:name=".ui.samples.keyboard.KeyboardScrollImeComposeActivity" />
        <activity android:name=".ui.samples.fullscreen.FullscreenCutoutComposeActivity" />
        <activity android:name=".ui.samples.fullscreen.FullscreenStableComposeActivity" />

    </application>
</manifest>